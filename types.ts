export interface CustomResponse {
  data: object;
  success: boolean;
  error: boolean;
  message: string;
}
export interface CustomErrorResponse {
  success: boolean;
  error: boolean;
  message: string;
}
